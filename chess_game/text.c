        //Get Mouse Position Here
        //Vector2 mouse=GetMousePosition(); 

        //DrawText("Congrats!",400,300,50,LIGHTGRAY);        
        //DrawCircle(circleX,circleY,RADIUS,RED);

/*        
        circleX+=Xdirection*speed*GetFrameTime();
        if(circleX>=800){
            Xdirection=-1;
        }else if(circleX<=0){
            Xdirection=1;
        }

        circleY+=Ydirection*speed*GetFrameTime();
        if(circleY>=450){
            Ydirection=-1;
        }else if(circleY<=0){
            Ydirection=1;
        }
*/

/*
        //Left-Right movement
        if(IsKeyDown(KEY_RIGHT) && circleX<SCREEN_WIDTH-RADIUS){
            circleX+=speed*GetFrameTime();
        }
        if(IsKeyDown(KEY_LEFT)&& circleX>0+RADIUS){
            circleX-=speed*GetFrameTime();
        }
        //Up-Down movement
        if(IsKeyDown(KEY_DOWN)&&circleY<SCREEN_HEIGHT-RADIUS){
            circleY+=speed*GetFrameTime();
        }
        if(IsKeyDown(KEY_UP)&&circleY>0+RADIUS){
            circleY-=speed*GetFrameTime();
        }
*/
/*
        //Mouse Controls
        if(IsMouseButtonDown(MOUSE_BUTTON_LEFT)){
        DrawCircleLines(pos.x,pos.y,RADIUS,RED);
        printf("X:%.0f || y:%.0f\n",pos.x,pos.y);
        }        
*/
        //Rectange
/*
        Rectangle square={100,100,100,100};

        if(CheckCollisionPointRec(mouse,square)&&IsMouseButtonPressed(MOUSE_BUTTON_LEFT)){
            clickState*=-1;
        }
        if(clickState==-1){
            squareColour=BLUE;
        }else{
            squareColour=RED;
        }
        DrawRectangleRec(square,squareColour);
*/
